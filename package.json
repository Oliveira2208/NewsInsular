{
  "name": "newsinsular",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:web": "cd apps/web && next dev",
    "dev:mobile": "cd apps/mobile && expo start",
    "build:web": "cd apps/web && next build"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}